{
  "shape_id": "keys-map-4",
  "rationale": "Fingerprint matches `a => { let e = JSON.parse(a).images; return Object.keys(e).map(...) }`. Feed `'{\"images\":{}}'`: `Object.keys({}) = []`, `.map(...) = []`. Rust `split_lines+filter_empty+json_extract(sentinel)` yields `[]`. Both empty.",
  "expected_transforms": [
    "split_lines",
    "filter_empty",
    { "type": "json_extract", "name": "__sentinel_no_field__" }
  ],
  "inputs": [
    "{\"images\":{}}"
  ]
}
