{
  "shape_id": "parse-map-8",
  "rationale": "Fingerprint `JSON.parse(o).map(e => ({name: e.id.substring(0,7), ...}))`. Feed `\"[]\"`: JS `.map(...) = []`. Rust `split_lines+filter_empty+json_extract(sentinel)` on `\"[]\"` yields `[]`. Both empty.",
  "expected_transforms": [
    "split_lines",
    "filter_empty",
    { "type": "json_extract", "name": "__sentinel_no_field__" }
  ],
  "inputs": [
    "[]"
  ]
}
