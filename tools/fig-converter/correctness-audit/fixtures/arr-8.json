{
  "shape_id": "arr-8",
  "rationale": "Both `arr-8` generators start with `if (e.trim() == \"\") return [];`. Feed `\"\"`: early return `[]`. Rust `split_lines+filter_empty+json_extract(sentinel)` on `\"\"` yields `[]`. Both empty.",
  "expected_transforms": [
    "split_lines",
    "filter_empty",
    { "type": "json_extract", "name": "__sentinel_no_field__" }
  ],
  "inputs": [
    ""
  ]
}
