{
  "name": "custom-unsupported",
  "description": "UX-9 Phase 5 fixture (parked until Phase 2 lands the js_runtime field on GeneratorSpec). A requires_js generator with a custom kind: no script — js_runtime.source is an async function that returns the suggestion list directly. Activating this fixture without Phase 2 fails GeneratorSpec's deny_unknown_fields.",
  "subcommands": [
    {
      "name": "items",
      "args": [
        {
          "name": "id",
          "generators": [
            {
              "requires_js": true,
              "js_runtime": {
                "kind": "custom",
                "source": "async () => [{ name: 'a' }, { name: 'b' }, { name: 'c' }]"
              }
            }
          ]
        }
      ]
    }
  ]
}
