{
  "name": "post-process-supported",
  "description": "UX-9 Phase 4 fixture (parked until Phase 2 lands the js_runtime field on GeneratorSpec). A requires_js generator with a post_process kind: the script is executed normally, then stdout is fed through the JS function in js_runtime.source. Activating this fixture without Phase 2 fails GeneratorSpec's deny_unknown_fields.",
  "subcommands": [
    {
      "name": "names",
      "args": [
        {
          "name": "name",
          "generators": [
            {
              "script": ["printf", "%s\\n", "hello world"],
              "requires_js": true,
              "js_runtime": {
                "kind": "post_process",
                "source": "out => out.split('\\n').filter(Boolean).map(name => ({ name }))"
              }
            }
          ]
        }
      ]
    }
  ]
}
