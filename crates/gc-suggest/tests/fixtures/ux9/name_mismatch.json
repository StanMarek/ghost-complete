{
  "name": "alias-target",
  "description": "UX-9 fixture: filename stem (`name_mismatch`) deliberately disagrees with `name` (`alias-target`). Phase 1 will use this to verify that command addressability follows the file path, not the JSON `name` field — so this spec stays addressable as `name_mismatch` even when the converter renames it.",
  "subcommands": [
    {
      "name": "noop",
      "description": "No-op subcommand for fixture testing"
    }
  ]
}
