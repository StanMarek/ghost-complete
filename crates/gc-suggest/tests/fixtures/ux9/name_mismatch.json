{
  "name": "alias-target",
  "description": "Filename stem (`name_mismatch`) deliberately disagrees with declared `name` (`alias-target`). Pins addressability via filename stem: this spec stays reachable as `name_mismatch` regardless of the JSON `name` value.",
  "subcommands": [
    {
      "name": "noop",
      "description": "No-op subcommand for fixture testing"
    }
  ]
}
