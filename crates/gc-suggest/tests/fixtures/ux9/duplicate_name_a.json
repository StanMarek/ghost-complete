{
  "name": "duplicate",
  "description": "First of two specs that both declare `name: \"duplicate\"`. Pairs with duplicate_name_b.json to exercise the alias-conflict surface: both files address by filename stem, the alphabetically-first wins the `duplicate` alias, and the collision is recorded as a `command_alias_conflicts` warning.",
  "subcommands": [
    { "name": "from-a", "description": "Subcommand contributed by duplicate_name_a.json" }
  ]
}
