{
  "name": "duplicate",
  "description": "UX-9 fixture: first of two specs that both declare `name: \"duplicate\"`. With the current name-keyed HashMap loader, only one of {a,b} survives (first-match-wins per directory). Phase 1 surfaces this as a `command_alias_conflicts` warning and addresses one via the file stem so neither is silently dropped.",
  "subcommands": [
    { "name": "from-a", "description": "Subcommand contributed by duplicate_name_a.json" }
  ]
}
